
import 'package:flutter/cupertino.dart';

class TabRoute extends StatelessWidget {
  const TabRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to is tab route"),
    );
  }
}